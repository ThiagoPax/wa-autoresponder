<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Tema básico (sem depender do Material Components) -->
    <style name="Theme.WAAutoResponder" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <!-- Opcional: ajustes simples -->
        <item name="android:windowBackground">@android:color/background_dark</item>
        <item name="android:forceDarkAllowed">true</item>
    </style>

</resources>
